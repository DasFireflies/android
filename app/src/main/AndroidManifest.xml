<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jhu.fireflies.com.clue_less">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LobbyActivity" />
        <activity
            android:name=".GameBoard"
            android:label="@string/title_activity_game_board"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".InvestigationPadActivity" />
        <activity android:name=".StartOfGame" />
        <activity android:name=".SuggestionAccusationActivity" />
        <activity android:name=".TextBased" />
        <activity android:name=".InitialClues" />
        <activity android:name=".ActivityLog"></activity>
    </application>

</manifest>